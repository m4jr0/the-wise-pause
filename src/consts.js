class Consts {
  static APP_NAME = GM_info.script.name
  static APP_VERSION = GM_info.script.version
  static MAIN_PREFIX = 'the-wise-pause-' // Prevent conflicts with current page.
  static MAIN_CONTAINER_ID = `${Consts.MAIN_PREFIX}main-container`
}
